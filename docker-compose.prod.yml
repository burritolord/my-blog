version: '3.5'

services:
  ghost:
    build: ./ghost
    container_name: ghost
    restart: unless-stopped
    expose:
      - '2368'
    volumes:
      - ./ghost/content:/var/lib/ghost/content

networks:
    default:
       external:
         name: nginx_proxy